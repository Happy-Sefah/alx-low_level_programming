#include <stdio.h>
/**
 *main - Entry point
 *
 *Return: Always 0 (Success)
 */

int main(void)
{
	char a;

	char b;

	a = 'c';
	b = 'C';
	while
		(a <= 'z') {
			putchar(a);
			a++;
		}
	while
		(b <= 'C') {
			putchar(b);
			b++;
		}
	putchar('\n');
	return (0);
}
